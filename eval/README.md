# 평가 데이터 (기획서 2.6절)

| 데이터 | 내용 | 파일 |
|---|---|---|
| D1 자체 시험 페이지 | 가상 브랜드 피싱·사기·불법 도박 각 20개(유형별 6개 템플릿 계열) + 헷갈리기 쉬운 정상 대조 30개(10계열) = 90개 | `d1_families.py`(계열), `generate.py`(생성), `d1/manifest.json`(정답·분할), `../testsites/html/d1/` |
| D2 공격 시나리오 | SSRF 4종, DNS 2종, 리다이렉트 2종, 하위 자원·팝업·다운로드·Service Worker, 프롬프트 인젝션, 저장형 XSS, 대용량 문서 = 15종 | `d2/scenarios.json`, `../testsites/html/d2/`, `../testsites/nginx.conf` |
| D3·D4 | 공개 정상 사이트 170개 이상, 공개 피싱 라벨 자료 | 다음 단계 |

## D1 구성

- **변형**: `static`(내용이 HTML에 있음), `delayed`(base64로 감춘 내용을 1.2~2초 뒤 스크립트가 그림 — 정적 검사로는 안 보임), `redirect`(3단계 HTTP 리다이렉트 뒤 도착), `jsnav`(스크립트로 다른 페이지로 이동). 계열 A·B는 4변형, 나머지는 3변형.
- **정답이 URL에 드러나지 않게** 파일 이름은 의미 없는 ID(`/d1/p<해시>.html`)다. 규칙·모델은 URL 경로를 보지 않는다.
- **가상 브랜드만** 쓴다. 폼은 전송되지 않는다.
- 정상 대조는 로그인·결제·환급 안내·배송 조회·합법 체육진흥투표권 안내·피싱 주의 기사·후원 계좌·게임 캐시 충전처럼 의심 유형과 헷갈리는 화면이다.

## 개발용/평가용 분할

| | 개발용(dev) | 평가용(eval) |
|---|---|---|
| 피싱·사기·도박 | 유형마다 계열 A·C·E (10개씩) | 계열 B·D·F (10개씩) |
| 정상 대조 | 계열 A·C·E·G·I (15개) | 계열 B·D·F·H·J (15개) |

- 같은 계열의 변형은 한쪽에만 있다(템플릿을 외워서 맞히는 것을 막음).
- 정상 페이지 개발용은 D1·D3를 합쳐 30개로 둔다(기획서). D3가 들어오면 D1 개발용 15 + D3 개발용 15, 나머지 D3는 평가용.
- **평가용은 규칙·모델·프롬프트를 고정한 뒤 한 번만 연다.** `run.py`는 `--split eval`에 `--frozen-evaluation`을 요구하고, 작업 트리가 깨끗해야 실행하며, 먼저 고정 기록(`results/<실행>-freeze.json`: 커밋·AI 모델)을 남긴다.
- 한계: 개발용과 평가용 템플릿을 같은 개발자가 같은 시기에 썼다. 규칙(rules/1)은 D1보다 먼저(시험 페이지 7개 기준) 만들어졌고, 평가용 결과를 보고 규칙을 고치지 않는다. 고치면 새 버전으로 다시 고정하고 이전 결과와 함께 보고한다.

## 실행

```bash
python eval/generate.py                       # D1 페이지·목록 다시 만들기(시드 고정, 같은 결과)
docker compose restart testsites              # nginx 설정(리다이렉트·D2 경로) 반영

# 조사자 계정(예: eval-runner)을 CLI로 만든 뒤
EVAL_USER=eval-runner EVAL_PASSWORD=... python eval/run.py d2              # 공격 시나리오 15종
EVAL_USER=eval-runner EVAL_PASSWORD=... python eval/run.py d1 --split dev  # 개발용 45개
# 모델 비교: AI_MODEL=laya docker compose --profile laya up -d  후 같은 명령에 --tag laya
```

- D2-14(XSS)의 콘솔 확인은 Playwright가 있으면 콘솔에서 사건 상세를 열어 대화상자가 뜨는지 본다(`worker/.venv`로 실행).
- 결과는 `results/`에 JSON으로 남는다. 모든 비율은 건수와 95% Wilson 신뢰구간으로 낸다(`metrics.py`).

## 결과 (개발용·D2, 2026-09-24)

`results/`의 JSON이 원자료다. 평가용(eval)은 아직 열지 않았다.

**D2 공격 시나리오**: `20260924T000247-rules-d2.json` → **15/15 차단**, 이후 정상 페이지 조사로 Worker 생존 확인.
첫 실행(`20260923T232333-rules-d2.json`)에서는 D2-15 대용량 문서가 Worker를 30분 넘게 멈춰 14/15였다
→ 수집 격리 실행(`worker/worker/isolation.py`, SC-CE-03)을 넣고 다시 실행.

**D1 개발용 45건** (규칙 rules/1, 비율은 건수·95% Wilson 신뢰구간)

| 구성 | 피싱 Recall | 사기 Recall | 도박 Recall | 정상→의심 오판정 | 정상 보류 | 동적 징후 포착 | 최종 목적지 식별 | 전체 보류 |
|---|---|---|---|---|---|---|---|---|
| 규칙만 | 10/10 | 10/10 | 10/10 | 0/15 (CI 0~20.4%) | 15/15 | 12/12 | 17/17 | 15/45 |
| 규칙 + Laya | 10/10 | 0/10 | 4/10 | 0/15 | 15/15 | 5/12 | 17/17 | 31/45 |
| 규칙 + Qwen3-1.7B | 10/10 | 3/10 | 1/10 | 0/15 | 15/15 | 6/12 | 17/17 | 31/45 |

- 규칙만: 사기 Precision 10/13 — 슬롯(G-C) 3건이 도박과 함께 사기로도 표시됨(입금 계좌 안내 문구).
- 정상 대조 15건이 모두 보류(UNKNOWN)다. 오판정은 없지만 담당자가 모두 직접 봐야 한다(업무 부담). 로그인·결제·계좌 안내처럼 헷갈리게 만든 페이지라 약한 징후가 걸린다.
- 두 모델 모두 사기·도박을 규칙과 다르게 보아(Laya: 사기→피싱, Qwen3: 도박→사기 등) 규칙이 맞힌 건을 보류로 돌렸고, 정상 페이지도 의심으로 답해 보류를 줄이지 못했다. **개발용에서 AI를 더하면 결과가 나빠졌으므로 기본값은 규칙만(`AI_MODEL=none`)으로 둔다.**

## rules/2 조정과 1차 평가 (2026-09-24)

**개발용으로만 조정** (`3b4656b`, rules/2): 약한 징후 1개(1점)뿐이면 BENIGN(`minor_signals_only`), 금전 요구는 요구 형태(요청 표현·비용 항목+금액)로만,
'환급·지원금·당첨'은 약한 미끼 신호, 도박 사이트의 입금·메신저 안내는 도박 맥락(0점).

**1차 평가** — 평가용 45건을 rules/2 고정 뒤 한 번 열었다(`results/20260924T021024-frozen-rules2-*`, AI 끔).

| | 개발용 45 (조정에 사용) | **평가용 45 (1차 평가)** |
|---|---|---|
| 피싱 Recall | 10/10 | **4/10** (95% CI 16.8~68.7%) |
| 사기 Recall | 10/10 | 10/10 (72.2~100%) |
| 불법 도박 Recall | 10/10 | 10/10 (72.2~100%) |
| 유형별 Precision | 10/10 · 10/10 · 10/10 | 4/4 · 10/10 · 10/10 |
| 정상→의심 오판정 | 0/15 | **0/15** (0~20.4%) |
| 정상 보류 | 3/15 | **12/15** |
| 동적 징후 포착 | 12/12 | 10/12 |
| 최종 목적지 식별 | 17/17 | 17/17 |
| 전체 보류 | 3/45 | 15/45 |

D2(rules/2): **15/15 차단** (`results/20260924T021649-rules2-d2.json`). D2-11 합격 조건의 허용 증거 목록에 PR #13에서 추가된
`video`(조사 녹화)를 넣은 뒤 다시 실행했다(내려받은 파일이 저장되지 않는지 보는 조건이라 녹화는 허용 대상).

**해석 (평가용 결과를 보고 규칙을 고치지 않았다)**
- 개발용 성능이 평가용으로 옮겨지지 않았다. 계열 단위 분할이 이런 차이를 드러내려고 만든 장치다.
- **웹메일 사칭 피싱(P-D) 3건이 BENIGN** — 걸린 신호가 비밀번호 칸 1개뿐이라 rules/2의 "약한 징후 1개는 BENIGN" 조정에
  해당했다. rules/1이었다면 보류였다. **보류를 줄이려는 조정이 놓침을 만든 사례**로 결과보고서에 적는다.
- **가상자산 지갑 피싱(P-F) 3건이 보류** — '복구 문구 12단어'를 민감정보 입력으로 보지 못했다.
- 정상 대조의 결제(N-B)·피싱 주의 기사(N-F)·펀드 안내(N-H)·게임 캐시(N-J)는 약한 징후가 겹쳐 보류됐다. 오판정은 0이다.
- 표본이 유형별 10개라 신뢰구간이 넓다. 정상 150개 이상(D3)이 있어야 오탐 목표(95% 상한 5% 이하)를 따질 수 있다.
- 이후 규칙을 바꾸면(rules/3) 이 평가용은 이미 열렸으므로, 새 계열로 만든 평가용이나 D3·D4로 다시 재고 1차 결과와 함께 보고한다.
